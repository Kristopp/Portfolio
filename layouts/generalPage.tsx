import { ReactNode } from "react";

type PageProps = { 
    meta?: SEOProps;
    children: ReactNode;
}


 const GeneralPage = ({ meta, children }: PageProps) => {
    return (
        <div>
            
        </div>
    )
}

export default GeneralPage;
